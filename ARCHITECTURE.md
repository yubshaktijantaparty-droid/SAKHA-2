# SAKHA Bot - System Architecture

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    SAKHA BOT SYSTEM                         │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │        WhatsApp Integration Layer                    │   │
│  │  (Baileys / Twilio / WhatsApp Business API)         │   │
│  └───────────────────┬──────────────────────────────────┘   │
│                      │                                        │
│  ┌──────────────────▼──────────────────────────────────┐   │
│  │        Message Processing Layer                     │   │
│  │  (Input Validation, Sanitization, Parsing)         │   │
│  └───────────────────┬──────────────────────────────────┘   │
│                      │                                        │
│  ┌──────────────────▼──────────────────────────────────┐   │
│  │    Command Router & Handler Layer                  │   │
│  │  (Command Parsing, Authorization, Routing)         │   │
│  └───────────┬──────────────────┬──────────────────────┘   │
│              │                  │                            │
│  ┌───────────▼──────┐  ┌────────▼─────────────────────┐   │
│  │ User Commands    │  │ Admin Commands               │   │
│  │ - AI Chat        │  │ - Ban/Unban Users            │   │
│  │ - Study Mode     │  │ - Broadcasting               │   │
│  │ - Notes/To-do    │  │ - Statistics/Analytics       │   │
│  │ - Reminders      │  │ - Bot Management             │   │
│  └────────┬─────────┘  └────────┬──────────────────────┘   │
│           │                     │                            │
│           └─────────────┬───────┘                            │
│                         │                                    │
│  ┌──────────────────┬──▼──────────────────────────────┐   │
│  │  AI Service Layer                                  │   │
│  │  (OpenAI / DeepSeek / Gemini)                     │   │
│  └────────────────┬───────────────────────────────────┘   │
│                   │                                         │
│  ┌────────────────▼───────────────────────────────────┐   │
│  │    Database Layer                                  │   │
│  │  (MongoDB / PostgreSQL)                            │   │
│  │  - Users, Conversations, Notes, Reminders, Todos  │   │
│  │  - Admin Logs, Analytics                          │   │
│  └────────────────────────────────────────────────────┘   │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │    Support Services                                 │   │
│  │  (Logging, Caching, Rate Limiting, Auth)           │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

## 📂 Project Structure

```
sakha/
├── config/                     # Configuration management
│   ├── __init__.py
│   └── settings.py            # Pydantic settings
│
├── database/                   # Database layer
│   ├── __init__.py
│   ├── models.py              # SQLAlchemy/Pydantic models
│   └── database.py            # Database operations
│
├── handlers/                   # Command handlers
│   ├── __init__.py
│   └── commands.py            # User & admin commands
│
├── ai/                        # AI service
│   ├── __init__.py
│   └── service.py             # AI providers (OpenAI, DeepSeek, Gemini)
│
├── whatsapp/                  # WhatsApp integration
│   ├── __init__.py
│   └── integration.py         # WhatsApp providers
│
├── admin/                     # Admin system
│   ├── __init__.py
│   └── system.py              # Admin management
│
├── utils/                     # Utilities
│   ├── __init__.py
│   └── helpers.py             # Helper functions
│
├── main.py                    # FastAPI application
└── __init__.py
```

## 🔄 Message Flow

```
WhatsApp
  │
  ├─→ Message Received
  │
  ├─→ Webhook Handler
  │
  ├─→ Validate Input
  │   └─→ Check length, sanitize
  │
  ├─→ Parse Command
  │   └─→ Extract command & args
  │
  ├─→ Command Handler
  │   ├─→ Check if admin command
  │   ├─→ Verify permissions
  │   └─→ Route to appropriate handler
  │
  ├─→ Handler Execution
  │   ├─→ AI Chat Handler
  │   ├─→ Study Assistant Handler
  │   ├─→ Admin Handler
  │   └─→ Utility Handler
  │
  ├─→ Process Response
  │   ├─→ Format response
  │   ├─→ Add emojis/formatting
  │   └─→ Save to database
  │
  ├─→ Send Response
  │   └─→ WhatsApp API
  │
  └─→ Log & Analytics
      └─→ Database
```

## 🔐 Security Layers

```
┌─────────────────────────────────────┐
│     Input Validation Layer          │
│  - Max length check                 │
│  - Character validation             │
│  - XSS prevention                   │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│     Authentication Layer            │
│  - Admin verification               │
│  - Role-based access control        │
│  - API key validation               │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│     Rate Limiting Layer             │
│  - Per-user rate limits             │
│  - IP-based rate limits             │
│  - Endpoint-specific limits         │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│     Moderation Layer                │
│  - Spam detection                   │
│  - Abuse filtering                  │
│  - Link blocking                    │
└────────────┬────────────────────────┘
             │
┌────────────▼────────────────────────┐
│     Error Handling Layer            │
│  - Safe error messages              │
│  - Logging without sensitive data   │
│  - Recovery mechanisms              │
└─────────────────────────────────────┘
```

## 🗄️ Database Schema Relationships

```
┌─────────┐
│  Users  │
│    *    │
└────┬────┘
     │ 1
     │
     ├─→ Conversations (1:N)
     │
     ├─→ Notes (1:N)
     │
     ├─→ Reminders (1:N)
     │
     └─→ Todos (1:N)

Admin Logs ──→ Users (optional)
Analytics  ──→ (Summary data)
```

## 🎯 Request Processing Pipeline

```
1. Receive Request
   ↓
2. Parse WhatsApp Message
   ↓
3. Extract User ID & Message
   ↓
4. Validate Input
   ↓
5. Get User from Database
   ↓
6. Check Rate Limits
   ↓
7. Parse Command/Message
   ↓
8. Route to Handler
   ├─→ If Command: Route to command handler
   └─→ If Text: Route to AI chat
   ↓
9. Execute Handler
   ├─→ Query Database if needed
   ├─→ Call AI Service if needed
   └─→ Generate Response
   ↓
10. Save Conversation to Database
    ↓
11. Update User Statistics
    ↓
12. Format Response
    ↓
13. Send via WhatsApp
    ↓
14. Log Event
    ↓
15. Complete
```

## 🚀 Scaling Architecture

```
Single Instance:
┌─────────────────┐
│   FastAPI App   │
│   + Bot Logic   │
└────────┬────────┘
         │
    ┌────▼─────┐
    │  MongoDB  │
    └───────────┘

Scaled Architecture:
┌──────────────┐
│   Nginx      │
│   (Load Bal) │
└──────┬───────┘
       │
       ├─→ ┌─────────────┐
       │   │ App Inst 1  │
       │   └─────────────┘
       │
       ├─→ ┌─────────────┐
       │   │ App Inst 2  │
       │   └─────────────┘
       │
       └─→ ┌─────────────┐
           │ App Inst N  │
           └──────┬──────┘
                  │
           ┌──────▼────────┐
           │  MongoDB      │
           │  (Replica)    │
           └───────────────┘
           
           ┌──────┬────────┐
           │ Redis│ Cache  │
           └──────┴────────┘
```

## 📊 Data Flow Diagram

```
User (WhatsApp)
      │
      │ Message
      │
      ▼
┌──────────────────┐
│ WhatsApp API     │
└────────┬─────────┘
         │
         │ Webhook
         │
         ▼
┌──────────────────┐
│ Message Handler  │
└────────┬─────────┘
         │
      ┌──┴──────────────┬─────────┐
      │                 │         │
      ▼                 ▼         ▼
  Command          Normal      Media
  Handler          Chat        Handler
      │              │            │
      │              ▼            │
      │         ┌──────────────┐  │
      │         │ AI Service   │  │
      │         └──────┬───────┘  │
      │                │          │
      └────────┬───────┴──────────┘
               │
               ▼
         ┌───────────────┐
         │ Database Ops  │
         │ Save/Update   │
         └───────┬───────┘
                 │
                 ▼
         ┌──────────────┐
         │ Format Resp. │
         └───────┬──────┘
                 │
                 ▼
        ┌────────────────┐
        │ Send WhatsApp  │
        └────────┬───────┘
                 │
                 ▼
            User ✓
```

## 🔧 Component Interactions

```
┌─────────────────────────────────────────────────────────┐
│                FastAPI Application                      │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Router                                                 │
│  ├─→ GET /health                                        │
│  ├─→ GET /api/users/{id}                               │
│  ├─→ POST /api/message                                 │
│  ├─→ POST /api/broadcast                               │
│  ├─→ POST /webhook/whatsapp                            │
│  └─→ POST /api/stats                                   │
│                                                          │
│  Middleware                                             │
│  ├─→ CORS Handler                                      │
│  ├─→ Error Handler                                     │
│  ├─→ Logging Middleware                                │
│  └─→ Rate Limiter                                      │
│                                                          │
└─────────────────────────────────────────────────────────┘
         │                    │                   │
         ▼                    ▼                   ▼
    Database          AI Service            WhatsApp
    Manager           (OpenAI, etc)         Manager
         │                    │                   │
         ▼                    ▼                   ▼
      MongoDB         API Clients         WhatsApp APIs
```

## 🎛️ Configuration Management

```
Settings (Pydantic)
    │
    ├─→ from .env file
    ├─→ from environment variables
    └─→ from default values
    
Used by:
    ├─→ Database initialization
    ├─→ AI service setup
    ├─→ WhatsApp configuration
    ├─→ Security settings
    ├─→ Feature toggles
    └─→ Server configuration
```

---

## Performance Optimization

1. **Async/Await** - All I/O operations are async
2. **Connection Pooling** - Database connection pool
3. **Caching** - Redis for frequently accessed data
4. **Rate Limiting** - Prevent abuse
5. **Pagination** - Limit data transfer
6. **Indexing** - Optimize database queries

## Monitoring & Logging

- Request/Response logging
- Error tracking with Sentry
- Performance metrics
- User analytics
- Admin action logging

---

This architecture ensures scalability, security, and maintainability.
