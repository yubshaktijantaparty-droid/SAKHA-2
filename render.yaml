# Render Build Configuration

# Node version
node_version = 18.0.0

# Python version
python_version = 3.12

# Preinstall commands
pre_build_commands = ["pip install --upgrade pip"]

# Environment variables are set in Render dashboard
